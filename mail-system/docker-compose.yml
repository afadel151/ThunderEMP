services:
  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: emp
      MYSQL_PASSWORD: emp123
      MYSQL_DATABASE: mail
    ports:
      - "3306:3306" 
    volumes:
      - mysql_data:/var/lib/mysql 


volumes:
  mysql_data: